cmake_minimum_required(VERSION 3.11)
project(OOP_indie_studio_2018)

set(CMAKE_CXX_STANDARD 17)
set(TARGET_NAME "bomberman")

set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS}")

if (APPLE)
    set(CMAKE_C_COMPILER "/usr/local/opt/llvm/bin/clang")
    set(CMAKE_CXX_COMPILER "/usr/local/opt/llvm/bin/clang++")
endif ()

find_library(IRRLICHT_LIBRARY REQUIRED)
#find_package(Irrlicht)

#include_directories(LibIrrlicht/include/)
include_directories(${IRRLICHT_INCLUDE_DIR})

add_executable(${TARGET_NAME} irrlichtTest/test.cpp)
target_link_libraries(${TARGET_NAME} ${OPENGL_LIBRARIES} ${GLUT_LIBRARIES} ${X11_LIBRARIES} ${IRRLICHT_LIBRARIES})

if (APPLE)
    target_link_directories(${TARGET_NAME} PRIVATE /usr/local/opt/llvm/lib)
endif ()